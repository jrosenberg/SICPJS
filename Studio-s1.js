// Type your program in here!
function biggie_size(x) {
    return x + 4;
}
biggie_size(1);

function unbiggie_size(x) {
    return x - 4;
}
unbiggie_size(5);

function is_biggie_size(x) {
    x>=5 ? true : false;
}
is_biggie_size(5);